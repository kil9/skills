---
description: kil9 스타일로 이슈/PR/코멘트 초안을 쓰거나 기존 초안을 그 말투로 다듬는다. "kil9처럼 써줘 / 내 스타일로 이슈·코멘트 써줘" 라고 할 때. 게시는 반드시 사용자 승인 후에 한다.
allowed_tools: [Read, Grep, Glob, Bash, Write, Edit, AskUserQuestion]
---

작성/변환 대상: `$ARGUMENTS`

너의 임무는 위 내용을 **kil9 스타일**의 이슈 본문 또는 코멘트 초안으로 만드는 것이다(또는 기존 초안을 kil9 말투로 다듬는 것).

- **산출물은 언제나 초안 파일로 낸다** — 채팅에 본문을 붙여 놓고 끝내지 않는다. 게시 여부와 무관하게 파일이 정본이고, herdr 안이면(`HERDR_ENV=1`) 옆 pane 에 vi 로 열어 사용자가 바로 고칠 수 있게 한다(경로·pane 레시피는 §9-1).
- **이슈 작성·코멘트 게시** 요청은 **draft → 승인 → 업로드** 흐름을 따른다. 사용자가 검토·수정·승인한 뒤에만 `gh issue create` / `gh issue comment` / `gh pr comment` 로 올린다. **예외 없음** — "바로 올려줘" 같은 요청에도 draft·승인 단계를 건너뛰지 않는다(§9 참조).
- **PR 본문·그 밖의 글** 은 초안 파일까지만 만들고 게시하지 않는다. 사용자가 검토 후 직접 올린다.
- **사용자가 초안을 고쳤으면 그 수정에서 스타일 규칙을 회수해 이 스킬에 반영한다**(§10). 초안 작업은 그대로 진행하고, 반영은 그 뒤에 한다.

원 분석 자료(STYLE-GUIDE·REPORT 전문)는 개인 repo 에 보관돼 있다. 아래는 그 핵심을 스킬에 자체 포함한 것이다.

## 0. 먼저 레지스터를 정한다

글의 종류에 따라 말투를 의식적으로 갈아탄다. 무엇을 쓰는지 모호하면 `AskUserQuestion` 으로 묻는다(이슈 본문인지 / 대외 코멘트인지 / 내부 분석 코멘트인지 / 완료 보고인지, 수신자가 타팀인지 내부인지).

- 이슈 본문 = 격식 합쇼체 사양서. 완결된 `~합니다`, 정형 헤더, 출처 인용, 체크리스트. 이모지·농담 최소.
- 대외/협업 코멘트 = 정중한 존댓말. 인사 → 답변 → 진행 약속 → 이견 요청. "~해보겠습니다", "업데이트 드리겠습니다".
- 내부 사고/분석 코멘트 = 음슴체 개조식(`~함/~임/~듯/~음`) + 🤔😅 + 말줄임표 `..`. 자문자답.
- 완료/종료 보고 = 초압축 한 줄. "배포 완료", "이슈 닫습니다."

## 1. 황금률 7가지

1. 레지스터를 먼저 정한다(위).
2. 판단엔 항상 여지를 남긴다. 결정/완료는 단정(`~합니다`), 판단/예측은 완곡(`~것으로 보입니다`, `~할 듯?`, `아마`). 모르면 `(추측)` 으로 정직하게 라벨링.
3. 추측은 데이터로 닫는다. "추측 → 로그/지표/벤치마크 확인 → 정량 수치 → 결론 → 한계". 막연한 표현 대신 측정값.
4. 출처/근거를 집착적으로 단다. 요청자는 본문 첫 줄 인용블록(`>`), 연관 이슈는 최상단 불릿, 코드 위치는 `파일:라인`.
5. 단기/장기를 나누고 리스크를 먼저 짚는다. "우선/일단 ~하고, 추후 ~". "나중에 ~한 장애가 날 수 있으니".
6. 영어 기술어는 그대로(`threshold`, `fallback`, `false positive`), 식별자/경로는 백틱.
7. 종료는 짧게. 명사형 한 줄.

## 2-4. 어미 매핑 · 정형구 · 본문 템플릿

실제 문장을 쓰는 단계에서 [`references/style-reference.md`](references/style-reference.md) 를 펴고 그 표·목록을 따른다 — 어미 매핑표(§2), 정형구 라이브러리(§3), 본문 템플릿(§4)이 거기 있다. 초안을 한 줄이라도 쓰기 전에 읽는다.

## 5. 코멘트 톤

- 분석 코멘트는 `## TL;DR`(결론 먼저) → 근거(표/수치) → `## 한계`.
- 협업 요청: `@{대상} (cc: @{참조자})` 첫 줄 단독 → 줄바꿈 → "{이름}님, ..." → "혹시 ~해주실 수 있을까요? 😄".
- 의견: 근거 먼저, 제안은 완곡하게, 마지막에 "어떻게 생각하시나요? 다른 방법 제안도 좋습니다." 로 결정권을 넘긴다.

## 6. 문장부호·포맷팅

- 말줄임표는 점 2개 `..` (표준 `...` 아님).
- 이모지 절제: 🤔(의문) / 😅😓(멋쩍음) / 😄😊(부탁) / 🎉(성과) / 👍✅(완료) / ⚠️(경고). 한 코멘트에 1개 정도.
- 느낌표 거의 안 씀. 물음표는 자문에.
- **em-dash(`—`) 절대 금지.** 범위/연결은 `-` 와 `→`. 범위는 `14-15%`, `3-5개`.
- 나열·구분 기호는 슬래시 `/`. 가운뎃점 `·` 는 쓰지 않는다(`branch·event` → `branch/event`). 범위는 여전히 `-`.
- 약어는 영문으로(`e.g.`, `i.e.`). 한글 약어(`예:`)는 지양.
- 볼드는 핵심 수치/결정에만 1-2개. 이탤릭 안 씀(필요하면 작은따옴표). 식별자/경로는 백틱.
- 비교/벤치마크는 표. 긴 부록은 `<details>`. 취소선은 농담/철회한 생각에만.
- `.md` 출력에서 범위용 `~` 는 `\~` 로 escape(GFM 취소선 오인 방지).
- AI 생성 푸터(`🤖 Generated with [Claude Code](...)`)는 넣지 않는다. 초안에 들어 있으면 지운다.

## 7. 다듬기: 군더더기 덜어내기

에이전트 초안이나 1차 작성본을 정본으로 옮길 때, 의미를 바꾸지 않는 선에서 덜어낸다. (이 스킬이 "기존 초안을 kil9 말투로 다듬을 때" 특히 중요하다.)

- 내가 하는 작업/결정의 서술은 단정형으로. "~하는 방향입니다", "~하려고 합니다" 같은 의도·완곡 표현을 빼고 `~합니다`. (판단/예측의 완곡 어미는 §1-2 대로 유지 — 여기 규칙은 "내가 하는 일"을 적을 때만 적용한다.)
- 인과는 가볍게. "~던 탓에" 류 책망조·문어체 연결 대신 "~어서", "~니까".
- 장황한 설명절은 짧은 부사로 압축한다. "코드 문제가 아닌데도 리포트가 생성되어" → "의미없이".
- 자명하거나 부차적인 정당화 문장은 통째로 삭제한다. 결정은 남기되 "왜"를 길게 늘어놓지 않는다.

## 8. 마무리

초안을 내놓기 전 §0-§7 에 어긋난 곳이 없는지 훑고 고친다.

placeholder(`{팀}`, `{이름}`, `@{id}`, `#NNNN`)는 사용자가 준 실제 값으로 채운다. 모르는 값이 있으면 placeholder로 두고 사용자에게 채우라고 알린다. 초안 작성 후, 어떤 레지스터로 썼는지와 사용자가 확인·수정할 부분을 한 줄로 안내한다. **단, 여기서 멈추지 말고 §9(초안 파일·vi pane, 게시 요청이면 승인·업로드)와 §10(사용자 편집 회수)을 이어서 수행한다.**

## 9. 초안 파일 · 업로드 흐름 (draft → 승인 → 업로드)

**초안 파일을 만들기 전에 [`references/upload-flow.md`](references/upload-flow.md) 를 읽고 그 절차를 수행한다.** 파일 경로·`.orig` 스냅샷·herdr vi pane 열기(§9-1)는 게시하지 않는 글에도 그대로 적용한다. 게시 요청이면 그 파일이 승인 게이트이기도 하다 — 사용자가 편집·승인한 뒤에만 `gh` 로 올린다. 읽지 않고 바로 `gh issue create` / `gh issue comment` / `gh pr comment` 를 실행하면 승인 없이 외부에 글이 올라간다.

(PR 본문 등 게시하지 않는 글은 §9-1 까지만 하고 §9-3 업로드는 건너뛴다.)

## 10. 사용자 편집 회수 (초안 → 스킬 반영)

초안을 정리하기 전에 `diff -u {초안}.orig {초안}` 으로 사용자가 무엇을 고쳤는지 본다. **차이가 없으면 여기서 끝이다.**

수정이 있으면 각 hunk 를 하나씩 판정한다.

- **반영**: 규칙 한 줄로 서술되는 것 — 어미·문장부호·이모지·포맷팅·구조(섹션 순서, 헤더 유무)·군더더기 삭제 패턴. 기존 규칙과 정면으로 어긋나는 수정도 반영 대상이다(사용자 쪽이 정본이니 그 규칙 줄을 고친다).
- **무시**: 사실·수치·고유명사 정정, 그 글에만 해당하는 내용 추가·삭제, 오타, 일반화되지 않는 일회성 표현 취향. **애매하면 무시한다** — 같은 수정을 두 번째로 보면 그때 반영한다.

반영은 이 `SKILL.md` §0-§7 또는 [`references/style-reference.md`](references/style-reference.md) 의 해당 표·목록에 한다. 새 줄을 덧붙이기 전에 **같은 것을 말하는 기존 줄이 있는지 먼저 찾고**, 있으면 그 줄을 고친다(규칙이 둘로 갈라지면 다음 초안에서 어느 쪽을 따를지 모호해진다). 규칙은 짧게, 필요하면 `전 → 후` 예를 한 쌍만 붙인다.

repo 규칙상 `codex/skills/kil9-writing-style/` 미러도 같은 수정을 얹는다(도구명 치환만 다르다). 마지막에 무엇을 반영하고 무엇을 무시했는지 한 줄로 보고한다. 커밋은 사용자가 요청할 때만 한다.
