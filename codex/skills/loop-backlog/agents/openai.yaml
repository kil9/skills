interface:
  display_name: "Backlog Loop"
  short_description: "Drain ready backlog tasks through Luna workers"
  default_prompt: "Use $loop-backlog to drain every self-actionable backlog task, delegate lean packets through $lunamax-threads, parallelize independent write-safe packets, and stop after two empty re-reads."
