---
name: add-draft
description: 아직 착수하지 않을 아이디어를 보류 항목으로 기록한다. "나중에 해보자 / 일단 적어만 둬 / 아이디어 하나" 처럼 보류를 요청할 때. 지금 할 일로 등록하는 것은 $add-task 다.
---

추가할 아이디어: `$ARGUMENTS`

아직 착수하지 않을, 나중에 정식 태스크로 편입할 후보를 보류 항목으로 기록한다. 승격(draft/아이디어 →
태스크)은 이 스킬이 아니라 `$add-task` 의 몫이다.

공통 전제(설치 명령·`--plain` 규칙·상태 4종)는 [`../references/backlog-basics.md`](../references/backlog-basics.md). `backlog/` 가 없는 새 저장소면 `$init-backlog` 으로 먼저 초기화하도록 안내한다.

## 하지 않는 것

- 구현·커밋을 하지 않는다.
- `request_user_input` 인터뷰를 하지 않는다(아이디어는 보류가 기본 — 정보가 부족해도 그대로 기록만 한다).
- 완료 조건(AC)을 강제하지 않는다. 착수 시 상세 인터뷰에서 채운다.

---

## 절차

**CLI 없으면 중단.** `command -v backlog` 로 확인하고, 없으면 파일 손편집으로 폴백하지 말고 설치를 안내한 뒤 멈춘다.

사용자 구상을 **손실 없이** 담아 draft 로 기록한다(승격 시 인터뷰·구현의 출발점이 되도록).

```
backlog draft create "<짧은 제목>" \
  -d "<사용자 구상 무손실 기록>

접수: <오늘 날짜>. 착수 전 상세 인터뷰 필요."
```

명백한 기술적 충돌·전제(예: 순수 클라이언트 정적 사이트라 서버·API 키가 없음 등)가 보이면 `-d` 본문에
"착수 시 검토 필요: <메모>" 를 덧붙인다. 항목을 제대로 적을 만큼만 가볍게 조사한다(관련 파일 확인 정도).

생성한 draft 의 ID·제목을 알리고 끝낸다.
