interface:
  display_name: "Plan Loop"
  short_description: "Drain ready PLAN tasks through Luna workers"
  default_prompt: "Use $loop-plan to drain every self-actionable PLAN task, delegate lean packets through $lunamax-threads, parallelize independent write-safe packets, and stop after two empty re-reads."
