---
name: add-milestone
description: 여러 태스크로 이뤄진 마일스톤을 인터뷰로 확정해 태스크와 함께 생성한다. "마일스톤 추가 / 이 큰 작업 계획 잡아줘" 라고 할 때. 태스크 하나면 $add-task, 저장소 첫 계획은 $init-backlog 다. 작업은 시작하지 않는다.
---

추가할 마일스톤: `$ARGUMENTS`

공통 전제(설치 명령·`--plain` 규칙·상태 4종)는 [`../references/backlog-basics.md`](../references/backlog-basics.md). 구현·커밋은 하지 않는다 — 마일스톤과 태스크만 만든다. `backlog/` 가 없는 새
저장소면 `$init-backlog` 으로 먼저 초기화하도록 안내한다.

## 규모 판별 (선행)

항목을 서술할 만큼만 가볍게 조사한 뒤 규모를 **재량으로 판정한다**(묻지 않는다 —
`../references/backlog-basics.md` 의 '분할은 묻지 않는다').

- 작업이 **태스크 하나로 끝나면** `$add-task` 로 위임하고 이 스킬은 종료한다. 위임했다는 사실을
  보고에 남긴다.
- 그 외에는 아래 절차로 계속한다. 태스크 1개짜리 마일스톤도 유효하다(개발 과정을 마일스톤 단위로
  격리하고 싶은 경우) — 사용자가 마일스톤을 명시적으로 요청했다면 그쪽을 존중한다.

## 절차

**CLI 없으면 중단.** `command -v backlog` 로 확인하고, 없으면 파일 손편집으로 폴백하지 말고 설치를 안내한 뒤 멈춘다.

1. 기존 마일스톤·선행 태스크를 `backlog milestone list --plain`·`backlog task list --plain` 으로
   확인한다(이름 중복 방지).
2. 마일스톤의 목표·범위와 **태스크 분해안**(각 태스크의 완료 조건·의존·우선순위·단독실행)을
   정리하고, 애매한 점이 남으면 모두 해소될 때까지 `request_user_input` 으로 인터뷰한다. 줄글 자유
   입력은 쓰지 않는다.

   **분해안(태스크 수·경계)은 묻지 않는다** — `../references/backlog-basics.md` 의
   '분할은 묻지 않는다'.
3. 확정 내용으로 마일스톤을 먼저 만들고, 각 태스크를 `-m` 으로 배정해 생성한다:

   ```
   backlog milestone add "<이름>" -d "<목표·범위>"
   backlog task create "<제목>" -d "<배경·접근>" \
     --ac "<완료 조건 1>" [--ac ...] -m "<마일스톤 이름>" \
     [--dep task-N[,task-M]] [--priority high|medium|low] [-l solo]
   ```

   태스크마다 `--ac` 1개 이상 필수, `-m` 누락 금지. 마일스톤 안팎의 순서 제약은 태스크 `--dep` 로
   표현한다(마일스톤 자체엔 의존 필드가 없다).

생성한 마일스톤 이름과 태스크 ID·제목 목록을 알리고 끝낸다.
